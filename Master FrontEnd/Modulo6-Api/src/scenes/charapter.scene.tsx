import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharapterContainer } from 'pods/charapters';

export const HotelScene = () => (
  <AppLayout>
    <CharapterContainer />
  </AppLayout>
);
