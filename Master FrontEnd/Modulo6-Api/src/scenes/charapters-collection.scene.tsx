import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharapterCollectionContainer } from 'pods/charapters-collection';

export const HotelCollectionScene = () => (
  <AppLayout>
    <CharapterCollectionContainer />
  </AppLayout>
);
